{
  "private": true,
  "name": "webpack-docs",
  "scripts": {
    "start": "gitbook serve",
    "build": "gitbook build && cp ./CNAME _book/",
    "release": "yarn build && gh-pages -d _book"
  },
  "author": "Eleven",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "gitbook-cli": "^2.3.2"
  }
}
